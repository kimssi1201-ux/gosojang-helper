{
  "name": "gosojang-helper",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev public --compatibility-date=2026-06-25",
    "start": "wrangler pages dev public --compatibility-date=2026-06-25",
    "deploy": "wrangler pages deploy public --project-name=gosojang-helper",
    "check": "node tools/check-files.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
